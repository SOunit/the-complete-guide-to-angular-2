# setup bootstrap

- import package

```
npm i --save bootstrap@3
```

- add to config, `angular.json`

```
"styles": [
              "node_modules/bootstrap/dist/css/bootstrap.min.css",
              "src/styles.css"
            ],
```
